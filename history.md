# history.md

## 2026-04-30

- 当前工作目录以 `D:/Codex/midTime/SCAU/JAVA_PRO` 为准，任务目标是完成组员 B 对主界面、节点交互、结构树同步与界面优化相关职责。
- 协作署名规则：每位成员只在项目文档中填写自己的真实名字，不代填他人信息。
- 变更追溯规则：修改已有作者文件时，按 `AGENTS.md` 追加该文件头部 `Changelog`；新建文件时按 `Script Header Template` 填写作者、时间、依赖、用法与初始记录。
- Git 提交规则：提交说明按 `AGENTS.md` 的 `feat / fix / add / refactor / docs / test / style / chore / perf / ci / build / revert` 规范执行，并参考 `origin/czb` 分支当前清晰具体的提交风格。
- 本次已在 `wwh/task-b-ui` 分支完成一组轻量任务 B 改动：增加显式“取消选中”入口，补齐选中取消后的属性弹窗与结构树同步，保留原项目可运行状态，并已提交推送为 `feat(ui): add explicit deselection controls`。
- 已按任务 B 代码实现补充论文模板 `附件3-2026春-面向对象程序设计实践-论文撰写格式及封面.doc` 中“系统实现”和“系统运行界面”对应内容，重点覆盖主界面布局、节点选中/取消选中、树与绘图区同步、提示信息与截图建议。
- 已继续完善任务 B 交互细节：结构展示区支持空白点击取消选中、双击节点重命名，状态栏补充操作提示，便于演示主界面与结构区联动能力。
- 已继续补强结构展示区交互：新增展开全部、定位中心、清空选择快捷操作，使结构区本身更像完整功能区，便于课堂演示和节点层级浏览。
- 验收检查确认项目可编译、可打包、可启动；`mvn -DforkCount=0 test` 能通过。为适配当前 Windows 环境，已在 `pom.xml` 中关闭 surefire forking，并同步修正 README 中的测试说明，使默认 `mvn test` 也可直接作为验收命令。
